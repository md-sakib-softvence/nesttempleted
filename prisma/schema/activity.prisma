model ActivityLog {
  activityLogId String   @id @default(uuid())
  title         String
  description   String
  role          Role
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([title])
}
