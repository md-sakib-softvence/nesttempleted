generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  ADMIN
  SUPER_ADMIN
  PLAYER
  FAN
  SECURITY_GUARD
  EMPLOYEE
  MANAGER
  AMBASSADOR
}

enum Status {
  ACTIVE
  TRIAL
  SUSPEND
}
